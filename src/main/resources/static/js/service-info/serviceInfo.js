/* var timeout = null;
var count = 0;

$(document).on('scroll', function(){
    count++
    $(window).scrollTop(937 * count);
  }); */

/* $(document).on('scroll', function(){
    clearTimeout(timeout);
    timeout = setTimeout(function(){
    var translateY = count++ * -937;
    console.log(translateY);
    $('.slide-wrapper').css('transform', 'translate3d(0px,' + translateY + 'px, 0px)');
    $('.slide-wrapper').css('transition-duration', '550ms');
    }, 200);
  }); */